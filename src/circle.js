class Circle {
    constructor(x, y, radius) {
        this.x = x;
        this.y = y;
        this.radius = radius;
        this.velocity = {x:0, y:0};
    }

    collidesWithRectangle(rectangle) {
        
    }
}

export { Circle };